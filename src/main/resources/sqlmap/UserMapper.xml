<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- dtd라는 파일을 참조하는 마이바티스 문서라는 것을 선언 -->

<!-- 구현 클래스의 위치 -->
<mapper namespace="com.team05.user.mapper.UserMapper">
	
	<select id="showmypage" resultType="Reserve_imgVO">
		select r.*,p.name,uploadpath,filename,filerealname,fileloca,tel from users u left join reservation r on u.id=r.id left join product p 
		on r.pro_no=p.pro_no where u.id=#{id}
	</select>
	<select id="getreview" resultType="Review_imgVO">
		select r.*,p.name,tel from users u left join review r on u.id = r.id left join product p on r.pro_no=p.pro_no 
		where u.id=#{id}
	</select>



</mapper>