<?xml version="1.0" encoding="UTF-8"?>
<!-- mapper dtd 작성 -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.team05.user.mapper.UserMapper">

	<!-- 회원가입 진행 -->
	<insert id="join">
		insert into users (id, pwd, name, email, email2, zipNo, addrBasic, addrDetail)
		values(#{id}, #{pwd}, #{name}, #{email}, #{email2}, #{zipNo}, #{addrBasic}, #{addrDetail})
	</insert>
	
	<!-- ID중복 확인 -->
	<select id="idCheck" resultType="int">
		
	</select>
	
	

	
</mapper>